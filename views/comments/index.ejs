<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Comments</title>
</head>
<body>
    <h1>Comments</h1>
    <ol>
        <% for(let comment of comments){  %>

            <h4><span><%= comment.username %></h4>
            <p><%= comment.comment %> <a href="/comments/<%= comment.id %>">Details</a>&nbsp;&nbsp;&nbsp;&nbsp;<a href="/comments/<%= comment.id %>/edit">Update</a></p>
            <form action="/comments/<%= comment.id %>?_method=DELETE" method="post">
                <button>Delete</button>
            </form>
        <% }                              %>
    </ol>
    <a href="/comments/new">New Comment</a>
</body>
</html>